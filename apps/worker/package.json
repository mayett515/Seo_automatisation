{
  "name": "@localseo/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "build": "tsc -p tsconfig.build.json",
    "start": "node --conditions=production dist/main.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsx --test src/**/*.test.ts",
    "test:integration": "tsx --test src/**/*.integration.ts"
  },
  "dependencies": {
    "@localseo/adapters": "workspace:*",
    "@localseo/ai": "workspace:*",
    "@localseo/config": "workspace:*",
    "@localseo/contracts": "workspace:*",
    "@localseo/db": "workspace:*",
    "@localseo/domain": "workspace:*",
    "bullmq": "5.79.1",
    "drizzle-orm": "0.45.2"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "4.22.4",
    "typescript": "6.0.3"
  }
}
