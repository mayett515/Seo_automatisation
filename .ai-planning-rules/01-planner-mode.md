---
description: "Planner mode for hidden-folder architecture and application blueprinting"
globs: "**/*.{md,json,mmd,toml,yml,yaml}"
alwaysApply: false
version: "1.0.0"
model_target: "universal-router-hybrid"
protocol_compat: "mcp: 2026-05"
dependencies: []
priority_schema: "critical > strong > guideline"
---

# Planner Mode

<meta-instruction>
Use this file before creating multi-file schemas, hidden rule folders, app architecture layouts, or major product planning artifacts.
</meta-instruction>

<positive-directives>
- Interrogate the target stack, data layer, type standards, coupling boundaries, and directory conventions before generating files.
- Produce a draft blueprint with both app folder layout and AI rule-folder layout.
- Ask what the user wants to re-decide, pivot, expand, or tighten before compilation.
- Proceed to file generation only after explicit approval.
- Verify rule-file globs against the proposed codebase tree.
</positive-directives>

<absolute-constraints>
- DO NOT skip the draft blueprint for large architecture or schema work.
- DO NOT compile rule files before the user approves the blueprint.
- DO NOT mix frozen reference material with editable project rules.
- DO NOT design hidden folders that overlap ownership without a clear router rule.
- DO NOT use Codex subagents where router modes are enough.
</absolute-constraints>

<context>
Current target stack:

```text
Backend: NestJS + Fastify
AI/workers: Mastra + deterministic queue workers
Frontend: React + TanStack
Data: PostgreSQL, Redis/BullMQ, object storage
Deploy: Netlify + GSC
```

Planner source reference:

```text
.ai-schema-factory/schema-files/for_planner_mode/PLANNER-MODE-SPEC.md
```
</context>

<pre-flight-checklist>
1. [ ] Did I ask only the questions needed to unblock the blueprint?
2. [ ] Did the blueprint show concrete folder paths?
3. [ ] Did I wait for explicit approval before file generation?
</pre-flight-checklist>

