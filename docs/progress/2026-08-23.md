# 2026-08-22/23 — Integration campaign, schema-guided refactor rounds, Claude-native adoption

## Database and integration truth

- First full real-PostgreSQL integration campaign since Docker/WSL2 became
  available on this machine. Five rounds took the suite from 46 failures to
  **312/312 green** (API 159, worker 153) against a disposable `postgres:17`
  container.
- Three append-only trigger-repair migrations landed, all fixing defects
  introduced in the 0047-0051 trigger program and invisible to unit tests:
  - `0052_fix_agent_run_workflow_identity.sql` — the run-level identity
    trigger referenced `NEW."output_ref"`, a column that exists only on
    `agent_run_steps`.
  - `0053_fix_agent_run_event_step_key.sql` — a PL/pgSQL variable named
    `step_key` collided with the selected column of the same name.
  - `0054_fix_jsonb_text_concatenation_precedence.sql` — unparenthesized
    `'prefix:' || jsonb->>'field'` expressions grouped as
    `(text || jsonb) ->> field`.
- Production fixes validated by the campaign: request-validation filter,
  queue-producer advisory lock with terminal-only archive, release media
  verification reading storage before `FOR UPDATE` (now pinned by a dedicated
  concurrency test), and ISO-`timestamptz` encoding for recovery liveness
  bounds in `work-recovery.ts` and `opportunity-research-execution.ts`.

## Schema-guided refactor rounds (nine cases, each adjudicated before commit)

- `classifyRankingProof` boolean flags -> honest `rank: number` (`0d98a4c`).
- `reports.service` facade evaluated and deliberately left unchanged
  (ADR 0021 + guard anchors; confirming existing code is a valid outcome).
- `buildReleasePreflightChecks` if-chain -> typed check table (`df5707c`).
- Local-page QA input -> discriminated union carrying registry-owned facts,
  proven identical across 12,289 input combinations (`3ea0939`).
- `recoveryJobSpec` six if-branches -> exhaustive kind-keyed table
  (`c5ffcd8`).
- Opportunity-research failure messages -> exhaustive
  `satisfies Record<OpportunityResearchFailureCode, string>` (`7b7b585`).
- Queue-job schemas -> shared `JobDataEnvelopeSchema`; the `.strict()` drift
  (Deploy/Rollback/WebsiteImport/OpportunityScout lax, six strict) is
  preserved and tracked as an open contract decision (`f6704f0`).
- Page-version tenancy join (10 hand-written copies) -> single owner
  `pageVersionProjectScope` in `packages/db`, banned per file by nine guard
  anchors (`1d75b21`).
- Deploy guard ladder duplicated with swapped middle branches -> one pure
  `decideProviderDeployAction` union; manual reconciliation now outranks a
  recorded `providerDeployId` per ADR 0009, closing a reachable path that
  auto-uploaded a manually stranded deployment (`63cc322`). Follow-ups in
  progress: replay-vs-manual precedence, API rollback queries missing the
  manual filter.

## Agent-layer changes

- Claude-native layer adopted verbatim from the pack master (`3eaa4fc`):
  `CLAUDE.md`, `.claude/rules|skills|hooks|agents`, `settings.json`; the one
  prescribed repo adjustment adds `archive/` to protect-paths.
- Library thresholds (ts-pattern at 4+ variants, `.exhaustive()`,
  neverthrow/Effect/Remeda criteria) lifted into the root `AGENTS.md`
  generic layer with two guard anchors (`f42f30b`).
- All host hooks now strip leading UTF-8 BOMs before parsing stdin
  (`af1b85f`): the Cursor CLI on Windows delivers hook payloads with a
  double BOM, which made the fail-closed pre-tool policy block every write.
- Guard anchors re-adjudicated after the campaign (`e729ffe`) and extended:
  tenancy-scope bans, deploy decision-order pin via `requireOrderedIncludes`,
  Claude-layer presence.

## Documentation

- This entry closes the gap the docs audit found: the fifteen commits above
  had shipped without progress or doc updates. Companion doc fixes update the
  ADR 0022 activation gate to migrations 0047-0054, integration-coverage
  counts to 312, dead `/C:/localseoproject/...` links, and the shipped
  deploy-decision backlog entry.

## Afternoon addendum (same day): worktree merges, efficiency round, vocabulary owners

- Worktree sessions merged: manual reconciliation now outranks replay with a
  DB-level markReleaseLive guard (`dbd0033`); the API refuses manually
  stranded deployments as rollback source/target upfront (`b11f464`); the
  dead classifyRankingProof export is deleted with its safety test pinned at
  rankingMilestoneForRank (`d3b9bcb`).
- Strict queue contracts: the four lax job schemas reject unknown keys after
  a full producer audit; the importRunId-carrying website-import producer
  parses through the schema, the dry-run branch deliberately does not
  (`45f3ebd`).
- Efficiency/robustness batch (dual-scout audited, docs-first applied):
  crawls survive per-page fetch failures with honest skip reasons
  (`5f1670a`); release verification bounds external body reads and fails
  honestly (`599a195`); hot API read paths batch instead of looping -
  release media manifests, knowledge search (151 to 4 queries), grouped
  opportunity counts, three Promise.all sites (`afdf543`); the deliberate
  SERP mock is labeled deliberate per ADR 0015 (`6b7fe3b`).
- Vocabulary owners: web api-path/error-message helpers deduped into
  apps/web/src/lib (`89fecb6`); web failures carry ApiError with status and
  code (`b5dd081`); the queue job-name vocabulary lives once in contracts as
  queueJobNames with routing derived from it, guard-anchored
  (`aedfc03`/`ff99660`).
- UI state honesty: the opportunity explorer models its last action as one
  discriminated union with run-completion invalidation at the query edge
  (`5e042d6`); the page-studio screen groups remote state per concern and
  lifts wiring into usePageStudioData, with the page-studio guard anchors
  re-pointed to the extracted hook (`beceff6`/`243d592`).
- Layer upkeep: .agents/rules mirrors the path-scoped rules for Antigravity
  (`052a87c`); layer precedence is stated in AGENTS.md (`07f3bd7`); agent
  mirrors are excluded from Prettier ownership (`75c9c4b`); a dormant
  per-file lint hook for interactive Antigravity sessions is committed
  (`09b443b`).
- Full integration suite re-proven green against disposable postgres:17
  after each behavior-affecting batch (finally API 162/162, worker 155/155).
