# Progress - 2026-07-17

## Bounded Physical Media Cleanup

- Added a worker-host maintenance scanner for the remaining ADR 0020 retention workflow; no cleanup API or new queue was introduced.
- Added durable compare-and-swap cleanup evidence to `media_assets`: attempt count, last attempt, active claim, completion, and bounded failure details.
- Successful ready/archived assets delete only their exact server-derived quarantine source. Their immutable normalized derivatives and durable asset rows remain untouched.
- Expired upload intents are eligible once the existing 24-hour expiry marks them failed. Other failed assets retain diagnostics for seven days, then may delete the exact quarantine source and bounded asset-specific derivative prefix only when no page-version projection references them.
- Added a separate bounded cleanup-storage listing port while keeping the existing JSON object-storage port unchanged. Filesystem and S3 adapters implement provider-neutral bounded prefix listing and idempotent deletion.
- Added pure retention decisions plus DB-backed tests for source-only cleanup, failed-prefix cleanup, page-version reference protection, idempotent retry after delete-before-commit crashes, stale-final-claim exhaustion, bounded provider failures, and competing scanner claims.
- Made invalid durable cleanup state observable in worker logs and documented the manual operator response for exhausted cleanup without adding a reset or delete API.
- Updated ADR 0018, ADR 0020, roadmap/status/testing docs, rule 15, and executable architecture guards in the same slice.

Next:

- Send the complete physical-cleanup slice to Fable before committing.
- Run the credentialed Page Proposal smoke when an external provider key is available.
- Return to the product roadmap with customer-safe Report and Next Action after cleanup is accepted.
