# Progress Log - 2026-07-07

## Architecture Regression Guards

- Converted repeated review findings into a project-rule shard:
  - `.ai-project-rules/15-architecture-regression-guards.md`.
- Routed the shard from `.ai-project-rules/00-system-index.md` for repeated review findings, persisted JSON seams, provider mutations, release live-truth projection, deploy artifact approval, and roadmap drift.
- Strengthened ADR 0010's regression guard with the concrete rule:
  - `releasePlans.status = "live"` must not be projected from `deployments.status = "provider_succeeded"`;
  - only post-deploy verification outcomes may project the coarse release plan to `live`.
- Added `tools/check-architecture-regression-guards.ts` and wired it into `corepack pnpm text:check`.
- The executable guard currently:
  - fails if Opportunity Scout re-embeds `score` into strict `evidenceJson`;
  - fails if the strict persisted evidence parse regression test disappears;
  - fails if deploy artifact approval guards or their tests disappear;
  - warns, but does not fail, for the known open release-spine seam:
    - inline GSC sitemap submission in `POST /verify`.
- When those open seams are fixed, the script should be tightened from warning to failure so the old behavior cannot return.

## Release Status Hardening

- Stopped the deploy worker from projecting `releasePlans.status = "live"` when the provider deployment reaches `provider_succeeded`.
- Kept `deployments.status = "provider_succeeded"` as transport truth and left the release plan in `deploying` until post-deploy verification reports `live_healthy` or `live_with_warnings`.
- Moved `releasePlans.deployedAt` onto the verification-live projection so the timestamp matches observed live health rather than provider acceptance.
- Tightened `tools/check-architecture-regression-guards.ts`: provider-success-to-release-live is now a failing regression guard instead of a warning.
- GSC verify workerization remains the known open release-spine seam: `POST /verify` still performs Search Console handoff inline until the next workerization slice.

## Workflow Engine Research Distillation

- Reviewed the workflow-engine stealer findings for the release-verification/GSC handoff seam.
- Accepted the semantics, not the runtimes: durable parent run, named child checks, idempotent one-active execution, status/conclusion split, and evidence-before-projection.
- Recorded the implementation direction in `docs/architecture/lifecycle-truth-hardening-backlog.md`: keep BullMQ + Postgres, make `release_verifications` the durable run, keep `release_verification_checks` as child evidence, and let the worker own release-health projection.
- Confirmed the next slice should not introduce Temporal/Inngest/Trigger.dev or a generic workflow DSL.
