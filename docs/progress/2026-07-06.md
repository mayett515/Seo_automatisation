# 2026-07-06 Progress

## Opportunity Scout Real-Provider Smoke

- Verified the local smoke database has all 23 migrations applied.
- Started local Redis and a fresh worker host with the OpenCode Go env file loaded from outside the repo.
- Verified the OpenCode Go `/models` endpoint returned HTTP 200 with the local key, so the previous 401/entitlement blocker is closed.
- Seeded the deterministic Martines Opportunity Scout fixture.
- Started a smoke API on port `4101` with DB and Redis configured after the older API on port `4000` returned the expected dry-run response.
- Ran the real Opportunity Scout smoke through API -> BullMQ -> worker -> OpenCode Go -> Zod parse.

Result:

```text
runId: 4d953af2-b157-4e6c-ace0-c36578b24f75
provider: opencode_go
model: deepseek-v4-flash
status: failed
failureCode: output_schema_mismatch
latencyMs: 56161
usage: {"inputTokens":1740,"outputTokens":6128}
opportunity.count: 0
```

Interpretation:

- Infrastructure smoke passed: enqueue, queue delivery, real adapter call, run ledger, usage metadata, and no-partial-persistence behavior all worked.
- The model returned parseable JSON, but used a simplified brief shape instead of the `OpportunityScoutOutput` contract.
- The next prompt-tuning slice should add a compact canonical output skeleton and stronger schema instructions; QA and Zod should remain strict.

Artifact:

- [2026-07-06-deepseek-v4-flash-schema-mismatch.md](../testing/scout-runs/2026-07-06-deepseek-v4-flash-schema-mismatch.md)

## Opportunity Scout Prompt Schema Hardening

- Added a canonical `OpportunityScoutOutput` example in `packages/ai`.
- The example is parsed by `OpportunityScoutOutputSchema` in the AI test suite so the prompt example cannot drift silently from the contract.
- Embedded the canonical example in the Opportunity Scout prompt.
- Added explicit prompt rules for:
  - copying `projectId` exactly from the input packet,
  - emitting full `EvidenceRef` objects instead of evidence IDs,
  - using the contract enum vocabularies,
  - omitting optional fields instead of emitting `null`.
- Kept Zod schemas, QA gates, worker persistence, and adapter behavior unchanged.

Validation:

```text
corepack pnpm --filter @localseo/ai test       pass, 35/35
corepack pnpm --filter @localseo/ai typecheck  pass
```

## Opportunity Scout Schema-Hardened Smoke

- Reset the synthetic Martines fixture to one reviewed seed ranking proof.
- Removed two local ad-hoc proof rows from the smoke project so the rerun matched the documented fixture.
- Reran the real Opportunity Scout smoke through the same OpenCode Go path with `deepseek-v4-flash`.

Result:

```text
runId: 507c7b57-29d9-4d0f-8594-62986d6e832b
provider: opencode_go
model: deepseek-v4-flash
status: succeeded
latencyMs: 48776
usage: {"inputTokens":3190,"outputTokens":6526}
opportunity.count: 2
opportunity.classifications: {"proven_win":1,"near_term_target":1}
```

Persisted summary:

```text
proven_win       Dachdecker / Markt Indersdorf / dachdecker markt indersdorf / monitor / score 100
near_term_target Entruempelung / Dachau / entruempelung dachau / create_brief / score 45
```

Interpretation:

- Prompt/schema hardening fixed the original simplified-shape failure.
- The final model output passed Zod and deterministic QA without weakening validation.
- The `proven_win` opportunity used only reviewed manual `ranking_proof` evidence.
- The GSC-only Dachau opportunity stayed `internal_signal` evidence and did not become customer-safe proof.

Artifact:

- [2026-07-06-deepseek-v4-flash-schema-hardened-success.md](../testing/scout-runs/2026-07-06-deepseek-v4-flash-schema-hardened-success.md)

## Markdown Context Record Boundary

- Reviewed the Big Eater report `C:/big eater/markdown-context-db-strategy-local-seo-2026-07-06.md`.
- Checked `C:/gebäudeservicefirma/Seo/future-seo-growth-plan` as read-only field evidence for the existing
  YAML-frontmatter, XML-block, Markdown-shard pattern.
- Accepted the useful boundary:
  - Postgres remains operational product truth.
  - Markdown may later hold durable project memory, field-evidence summaries, reusable SEO lessons, research findings, and agent handoffs.
  - Generated indexes or vectors are derived search aids only.
- Added ADR 0016 to prevent future drift toward a Markdown shadow database.
- Updated the Agent-First MVP roadmap RAG section so future retrieval work starts from the context-record boundary and still waits for a real trigger.
- Recorded the future Scheme-viewer implication: a viewer can read Markdown records and generated indexes as documentation
  memory, but product state remains in Postgres.
- No `.ai-context-db` folder, vector store, or retrieval feature was created.

## Opportunity Explorer Decision UI

- Added a PATCH helper to the web API client.
- Added Opportunity Explorer lifecycle controls in the detail panel:
  - monitor,
  - hold,
  - reject with required reason,
  - reopen to new.
- The decision form uses the existing project-scoped `PATCH /projects/:projectId/opportunities/:opportunityId/status`
  endpoint and invalidates opportunity/run queries after a successful mutation.
- No DB schema, QA gate, or worker behavior changed.

Validation:

```text
corepack pnpm --filter @localseo/web typecheck  pass
```

## DeepSeek Flash / Pro Scout Comparison

- Ran six real-provider Opportunity Scout runs on the deterministic Martines fixture:
  - `deepseek-v4-flash` x3,
  - `deepseek-v4-pro` x3.
- All six runs reached `succeeded` through API -> Redis/BullMQ -> worker -> OpenCode Go -> Zod -> QA -> persistence.
- All six runs produced the same classification histogram:

```text
{"proven_win":1,"near_term_target":1}
```

Summary:

```text
deepseek-v4-flash  success 3/3, avg latency 63709 ms, avg output 8569 tokens
deepseek-v4-pro    success 3/3, avg latency 141956 ms, avg output 10234 tokens
```

Interpretation:

- `deepseek-v4-flash` remains the best current default for repeated Opportunity Scout smoke and baseline runs.
- `deepseek-v4-pro` remains a fallback for richer packets or deeper reasoning.
- `glm-5.2` remains reserved for later page/layout/content/report-quality comparison.

Artifact:

- [2026-07-06-deepseek-flash-pro-comparison.md](../testing/scout-runs/2026-07-06-deepseek-flash-pro-comparison.md)

## Page Registry / Page Studio Source-Of-Truth Synthesis

- Reviewed the Big Eater report `C:/big eater/page-registry-page-studio-stealer-findings-2026-07-06.md`.
- Confirmed the report sharpens the existing Page Registry and Page Studio direction rather than replacing it.
- Added proposed ADR 0017 for the next Fable review:
  - `page_versions.pageJson` is the render and approval source of truth,
  - `component_instances` is projection/note-anchor data only,
  - `packages/page-registry` owns section schemas, variants, metadata, and renderers,
  - `packages/domain/src/page-studio` owns pure movement/replacement/readiness decisions,
  - AI page work emits structured PageJson/PageProposalJson only, never arbitrary HTML/CSS/React.
- Updated the Page Registry architecture and roadmap docs with the implementation order before the page lane starts.
- Fable review accepted ADR 0017 after two required amendments:
  - migrate the existing production render path out of `packages/domain`/provider adapters into the page-registry release-render path,
  - retarget SEO preflight and rendering to typed PageJson with render-parity tests, including the robots/noindex safety gap.
- ADR 0017 is now Accepted with those amendments recorded.

## Pre-Page Registry Seam Repair

- Reviewed the repo-coherence findings from the fresh Fable session and had a second reviewer agent verify the sharpest claims.
- Fixed the worker/API JSON seam before PageJson work:
  - Opportunity Scout now stores `score` only in the queryable `opportunities.score` column.
  - `opportunities.evidenceJson` stores the strict `OpportunityBrief` shape so the Explorer API can parse and render the evidence panel instead of returning `null`.
  - The worker integration suite now asserts persisted evidence parses through `OpportunityBriefSchema` and has no embedded `score`.
- Hardened deploy artifact inputs before Page Registry work:
  - deploy artifacts for publish/update-style actions now require a page version id, `status = approved`, `approvedAt`, and `pageJson`;
  - redirect/remove/noindex-style actions remain allowed without a page version until ADR 0017's typed action-conditional artifact validation lands.
- Updated stale roadmap/README/design lines that still described shipped Explorer lifecycle buttons and old `evidenceJson` score semantics as future work.
- Deferred the larger GSC verification ownership fix: `POST /verify` still performs Search Console submission inline today, and should move to a worker-owned job in the next release-spine hardening slice.
