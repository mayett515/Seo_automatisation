# Progress - 2026-08-01

## Architecture Coherence Checkpoint

- Confirmed bounded physical media cleanup shipped in `380751b` after an external architecture review found no blocker. GitHub Actions run `30706022883` passed validation, Postgres integration, and browser-smoke jobs on that commit.
- Re-inventoried the runtime from routes, modules, worker handlers, package dependencies, schema, ADRs, rules, roadmap, and current status docs rather than relying on the chronological progress narrative.
- Confirmed the modular-monolith boundary still holds: web does not import DB/provider packages; `packages/domain` and `packages/seo` remain framework/provider free; workers own provider mutations and maintenance; APIs own authenticated commands and enqueue; AI output stays proposal-shaped behind deterministic gates.
- Confirmed the controlled page path is implemented through opportunity evidence, Page Proposal, append-only Page Studio edits, AI copy suggestions, media placement, preview, durable approval, release planning, deploy, verification, rollback, safe-work recovery, and physical media cleanup.
- Corrected current-facing drift that still described Page Registry, Page Studio, section notes, Page Proposal handoff, Better Auth wiring, and the production renderer as future work.
- Updated the architecture port inventory for `MediaAssetCleanupStoragePort` and added `MediaAsset` to the DDD-lite context list.
- Kept historical progress entries unchanged; point-in-time next steps remain historical evidence rather than current status.

## Structure Assessment

- No new service or microservice boundary is justified. The current `apps/web`, `apps/api`, `apps/worker`, and shared-package layout remains coherent.
- Large aggregate files such as `packages/contracts/src/index.ts`, `packages/ai/src/index.ts`, `packages/db/src/schema.ts`, `apps/api/src/modules/pages.module.ts`, and `apps/api/src/modules/releases.module.ts` are maintenance pressure, not an architecture failure. New report work should use dedicated report files/modules instead of widening page/release modules further; broad extraction should remain opportunistic and behavior-preserving.
- `component_templates`, `component_instances`, and `component_notes` remain unused scaffold tables rather than PageJson truth. The skeletal `reports` table and report route/task vocabulary are also not a report implementation.

## Customer Report Architecture Checkpoint

- Completed a focused pattern-mining synthesis across ten pinned repositories covering immutable revisions, claim/evidence provenance, digest-bound publication, artifact sealing, human action receipts, and database idempotency. No external code was copied; the adapted source record lives in `.ai-stealer-findings/2026-08-01-customer-report-publication-next-action.md`.
- Accepted ADR 0021: current operational rows own live truth; a canonical immutable JSON snapshot owns one historical customer statement; normalized rows enforce provenance/audit; stored HTML and later PDF are derivatives.
- Kept `PotentialReport` separate from project-owned `CustomerReport`, kept `report_narrative` draft-only, and prohibited AI from selecting facts, evidence, proof tiers, actions, targets, status, or publication.
- Fixed initial product defaults to monthly `de-DE` reports in `Europe/Berlin`; owner/admin may publish, editors may generate/review, viewers read published output only, and direct narrative editing/four-eyes publication remain deferred.
- Narrowed the first vertical proof to contracts/canonicalization, the core report aggregate, deterministic fact-only generation, review/request-changes, digest-bound human publication/correction, source-invalidation alerts, and immutable stored HTML. AI prose, consequential command offers, PDF, RAG, and generic workflow infrastructure are not prerequisites.
- Resolved the release-status design trigger without a migration: V1 reports read detailed approval/deployment/verification/check/rollback/recovery rows and never derive customer claims from coarse `release_plans.status` alone.

## Next

- Implement ADR 0021 slice 0: strict report contracts, canonicalization/property tests, pure lifecycle/eligibility decisions, explicit report permissions, and precise event semantics.
- Then add the report issue/run/version/provenance schema and real-PostgreSQL first-generation/publication race tests before building the fact-only assembler.
- Build the first report vertical from a direct server-owned evidence packet. Keep AI optional and RAG deferred until direct evidence loading proves insufficient.
- Use deployment and verification detail rows for customer-safe release claims; do not infer report truth from coarse `release_plans.status` alone.
- Run the credentialed Page Proposal smoke as a parallel operational calibration task when an external provider key is available; it does not block deterministic report-domain design.
