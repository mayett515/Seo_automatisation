# 2026-08-03

## ADR 0021 Slice 5: Authenticated Customer Report Workspace

- Added strict bounded operator workspace and candidate-detail contracts that expose canonical report truth and artifact summaries without storage keys.
- Added `report:review`-guarded workspace/candidate/staged-document reads; viewer access remains limited to published report reads through project membership.
- Added the TanStack report list, generation form, candidate review/request-changes/render-retry/publication controls, correction history, immutable candidate/published document frames, and typed Next Action navigation.
- Candidate and published HTML remain sandboxed stored artifacts; the API re-verifies immutable byte count and SHA-256 before either document response.
- Added contract, real-PostgreSQL service, pure UI-state, and 390px Chromium workflow coverage.
- A local severity-first pass added Europe/Berlin month-boundary defaults, visible active/failed generation rows before a candidate exists, deterministic initial/correction command selection, and a 100-row published-history bound matching the response contract.
- ADR 0021's first deterministic fact-only vertical is now implemented through Slice 5. AI narrative, command offers, PDF, RAG, public links, and direct prose editing remain deferred.

## Verification

- `corepack pnpm check` passes sequentially: formatting, text/architecture guards, diff check, lint, all workspace typechecks, migration drift, build, and non-DB tests. Lint retains only the known TanStack Table React Compiler compatibility warnings.
- The full Chromium suite passes all seven cases, including the 390px report review/publication workflow.
- Multiple local Opus print-mode attempts and the dedicated cloud ultrareview failed to return findings within their bounded timeouts; exact leftover review PIDs were checked and terminated without touching Claude Desktop or unrelated Codex processes. The final cloud session reported zero findings before timing out.
- Real-PostgreSQL report cases remain the GitHub Actions integration gate because `TEST_DATABASE_URL` is unavailable locally.
