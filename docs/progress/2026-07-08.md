# 2026-07-08

## Page Proposal UI Trigger And Status

- Added the operator-facing Page Proposal trigger in Opportunity Explorer:
  - selected opportunities can queue `POST /projects/:projectId/pages/proposals/runs`;
  - request and response bodies are parsed through the shared contracts;
  - rejected opportunities, already-created briefs, non-page-proposal recommendations, and active per-opportunity runs disable the action.
- Added Page Proposal run status to the frontend:
  - `AgentRunSummary` now exposes optional `subjectId`;
  - Opportunity Explorer reads `agent-runs?task=page_brief_draft`;
  - the selected opportunity panel shows the latest run for that opportunity;
  - the lower run area shows scout runs and page proposal runs separately.
- Kept the product boundary intact:
  - the UI queues durable work only through the API;
  - it does not call the model, worker, Mastra, or page persistence directly;
  - success still produces only draft page proposals and preview page versions through the worker.
- Switched Opportunity Explorer from route-ambiguous params to a typed route-owned `projectId` prop, matching the Pages screen pattern.
- Added an executable regression guard so the Page Proposal UI keeps parsing queue responses through contracts, calls the durable API endpoint, and reads status from `page_brief_draft` agent runs.

Follow-up in this same workday:

- Reviewed the Page Proposal UI/status slice.
- Built the approval flow for preview page versions, including unresolved `approval_blocker` enforcement.

## Page Version Approval Flow

- Added shared contracts for page-version review:
  - `ReviewPageVersionRequestSchema` accepts `approve` or `request_changes`;
  - requesting changes requires a decision note;
  - `PageVersionReviewResponseSchema` returns the updated page-version summary plus a durable approval audit record.
- Added `page:approve` as an explicit project permission for owner/admin/editor roles.
- Added `POST /projects/:projectId/pages/:pageVersionId/review`:
  - loads the page version through the project-scoped `pageVersions -> pageProposals` join;
  - parses and registry-validates the stored PageJson before review;
  - only reviews `preview` or `changes_requested` versions;
  - blocks `approve` while unresolved `approval_blocker` section notes exist;
  - sets `page_versions.status = approved` and `approvedAt` only for approvals;
  - writes `changes_requested` for request-changes decisions without approval evidence;
  - persists an `approvals` audit row tied to the actor, page version, decision, note, and timestamp.
- Extended the Pages preview surface:
  - loads page-version detail beside the sandboxed preview;
  - lists section notes through the existing notes query;
  - shows approval blocker state;
  - lets operators approve or request changes with visible pending/error/success feedback.
- Added integration coverage for:
  - successful approval and approval audit persistence;
  - unresolved `approval_blocker` notes blocking approval until resolved;
  - request-changes decisions not approving the page version;
  - request-changes note validation;
  - already-approved versions not being reviewed again.
- Updated ADR 0017, ADR 0019, the agent-first roadmap, the frontend/page-registry architecture doc, the Page Studio architecture note, backend foundation status, and the regression guard docs/script.

Next:

- Review the Page Version approval slice.
- Build release-plan creation from approved page versions, keeping release preflight and deploy as separate deterministic steps.
