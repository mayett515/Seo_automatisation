# 2026-08-09

## ADR 0022 Slices 1-7 - Opportunity Research Vertical

- Added strict workflow, step, event, evidence, business-profile, knowledge, public-search, opportunity-axis, research-state, and operator-timeline contracts.
- Extended `agent_runs` with versioned workflow identity and added typed step, append-only event, immutable evidence, and step-evidence persistence.
- Added database-owned transition, row-version, source-tenancy, proof-tier, lifecycle-evidence, and terminal-immutability triggers in migration 0047.
- Added a parent-run-first lifecycle ledger; evidence binding and an earlier database trigger instead pre-lock source rows in stable order before the run so source-dirty and recovery writers cannot form a lock cycle.
- Added revisioned project business profiles, canonical proposed/confirmed/rejected services and areas, completed-import confirmation, and actor-bound compare-and-set commands.
- Added versioned PostgreSQL Markdown knowledge with task scopes, typed links, human review, approved-version pointers, `simple` full-text search, and no embeddings/vector database.
- Changed ranking proof to `captured -> reviewed -> invalidated`; exact rank becomes customer-safe only after a persisted human review with expected status and row version.
- Added one durable Opportunity Research material resolver using confirmed profile/entities and eligible source rows, including GSC period identity rather than sync time alone.
- Added independent opportunity axes, deterministic lane derivation, canonical candidate keys, fixed 2/4/2 portfolio allocation without cross-fill, and visible shortfall truth.
- Added durable research readiness, pause intent, explicit rerun admission, `jobId = agentRunId`, queue-failure terminalization, and a bounded project-scoped run timeline.
- Kept legacy Opportunity Scout rows readable while new workflow opportunities may leave legacy `classification` and `score` null.
- Added the purpose-named `OpportunityResearchPort`, a typed V2 research-plan -> follow-up-capture -> strategy Mastra workflow, direct DeepSeek model adapter, and optional dedicated Mastra workflow store without exposing framework or provider types to product contracts.
- Added a separate DuckDuckGo HTML discovery adapter and immutable `public_web_search_captures`; discovery order remains research support and can never become Google rank or reviewed proof.
- Added bounded two-round research whose exact model-derived follow-up plan persists before capture, exact evidence citation with selected source-version checks, deterministic project-wide/same-run candidate dedupe before 2/4/2 persistence, due-only material/weekly scheduling, and ADR 0018 recovery under `jobId = agentRunId`.
- Added execution epochs and claim tokens to fence step/event/capture/terminal writes, exact recovery `job_runs` plus recovery-count ownership, atomic prior-epoch step reclamation, and stale provider-response rejection.
- Added content-addressed run-scoped candidate ids so equal-ranked duplicate resolution remains stable across crash retries.
- Added production fail-closed checks for DeepSeek, Mastra workflow storage, and public search configuration.
- Added the Opportunity Explorer research workspace for profile confirmation, knowledge review, ranking-proof review/invalidation, pause/resume/rerun, independent axes, portfolio shortfalls, exact citation projection, selectable run history, and bounded step/event/evidence timelines.
- Kept raw model output, tool bodies, storage keys, Mastra traces, and provider credentials out of operator responses.
- Moved application ORM query operators behind `@localseo/db/query` so API and worker composition share the database package's single Drizzle identity even when Mastra storage carries its own optional peer graph.
- Hardened every delivery against durable `job_runs` identity, including fencing the original transport by recovery generation after recovery ownership advances and archiving only superseded recovery-audit identities.
- Bound material identity to canonical evidence-packet bytes and exact initial queries, and bound captured-search evidence to execution epoch, region, and result-count request identity.
- Added migration 0050 so events/evidence carry durable execution epochs, project research state cannot drift from active run truth, and product success requires the exact ordered three-step actor/tool ledger plus final strategy digest.
- Added migration 0051 so every succeeded checkpoint preserves the exact canonical UTF-8 text whose SHA-256 identifies it; replay recomputes and verifies those bytes before reuse, and direct canonical-text or digest corruption is rejected.
- Recorded migration 0051's pre-activation requirement: migrations 0047-0051 must land before Opportunity Research workers are enabled because pre-0051 succeeded checkpoints cannot be truthfully backfilled from `jsonb::text`.
- Added exact-owner execution heartbeats and a separately counted recovery reservation. Heartbeats require the current run, epoch, claim token, and generation; recovery uses heartbeat-aware liveness and preserves a distinct audit row even when active transport appears after the claim.
- Preserved immutable succeeded checkpoints across recovery epochs while reclaiming only unfinished work; old provider responses remain unable to write under the new owner.
- Made research opportunities durable immutable exact projections of succeeded strategy candidates and their cited evidence ledger, rejecting cross-project sources, same-project fabricated product rows, semantic rewrites, and hard deletion.
- Separated human approval from model-use consent for project knowledge. Only the current, approved, non-retired version with a task-scoped `model_allowed` policy may enter model material; actor-bound compare-and-set retirement removes it non-destructively while preserving history.
- Added a deterministic pre-provider egress gate for obvious secret-shaped material and a version-bound promotion manifest/corpus covering workflow, policy, prompts, fixtures, and model identity.

## Independent Review Hardening

- Ran five independent read-only passes over runtime/migrations, documentation, hidden-rule routing, Big Eater pattern findings, and the project-relevant PoEAA cards. The reviews supported the existing Service Layer, Transaction Script, PostgreSQL lock-row/CAS, port-and-adapter, and durable-ledger direction; they did not justify a repository/UoW rewrite, microservice split, vector database, or generic MCP/tool catalog.
- Bound promotion identity to the exact deterministic fixture-corpus bytes and exposed the server-derived public-search evidence key to the model-visible normalized capture.
- Changed exact rerun replay to return its originally admitted material identity even after later project material drift, while refusing to fabricate V2 truth for a legacy active Opportunity Scout run.
- Added typed knowledge links to the operator write/read path while keeping traversal and agent-facing knowledge tools deferred.
- Replaced the latest database knowledge/proof guards so creation, review, invalidation, and declared source ownership require compatible same-project durable truth and actor roles even under direct SQL.
- Revalidated current source status, freshness, version, and source-specific admissibility every time immutable evidence is bound or rebound.
- Bound parent terminalization of pending steps to the parent's current execution epoch, including the pre-claim epoch-zero path.
- Made committed pre-enqueue Opportunity Research recovery reservations crash-reusable, retained the post-claim transport recheck, and made completed recovery transport without product truth terminalize visibly.
- Reloaded current project material before every DeepSeek provider attempt, retained the deterministic secret-shape gate for every retry, and prevented deterministic `model_egress_blocked` results from immediately scheduling the same material digest again.
- Removed hidden-rule dependency cycles, widened relevant rule globs to SQL/JSON artifacts, moved guards from superseded migration text to the latest installed function definitions, and added exact Mastra workflow-schema and promotion-corpus guards.
- Updated active roadmap, README, ADR, recovery, testing, and AI-runtime documentation so legacy V1 scout language and looser promotion/recovery claims no longer describe the shipped V2 boundary.
- Rechecked current durable source authority at both evidence-item and step-link boundaries, including explicit exclusion of operator-only knowledge from model citations.
- Added database-owned research-opportunity lifecycle truth, same-project page-proposal backing for `brief_created`, project-role checks for operator decisions, and fail-closed profile/entity confirmation evidence.
- Prevented a replacement business-profile revision from inheriting stale confirmed truth and kept actorless canonical entity rejection unavailable until that decision has modeled actor evidence.
- Bounded DeepSeek response bytes before JSON decoding, pinned production transport to the official HTTPS origin, and propagated execution-lease cancellation through the worker, Mastra workflow, and provider request.
- Kept deterministic model-egress failures from immediately rescheduling unchanged material, distinguished transport inconsistency from recovery exhaustion, and deferred a due V2 scan while any legacy scout still owns the shared task slot.
- Removed private ranking-proof screenshot keys from response contracts, reset decision forms on durable row-version changes, and refreshed ranking-proof/research queries after stale mutation failures.
- Made latest-function regression guards follow the Drizzle migration journal and inspect the exact final function body rather than trusting lexicographic filenames or superseded migration text.
- Recorded a frontend visualization boundary: explanatory animations and drawings are pre-authored versioned scenes with typed slots and reduced-motion/static fallbacks, never runtime model-generated SVG/layout programs.

## Verification

- Targeted typechecks passed for contracts, database, adapters, AI, API, worker, and web workspaces; migration drift reports 66 tables and no schema changes outside migrations 0047-0051.
- The complete DB-free unit gate contains 524 passing tests: tools 1, config 16, contracts 60, adapters 61, AI 58, domain 83, page-registry 26, SEO 7, API 80, worker 121, and web 11. Suites were executed sequentially without workspace fan-out.
- Chromium Opportunity Research operator acceptance passed 1/1 with exact rerun/proof command assertions, selectable run history, and no horizontal overflow at 390 px or 768 px.
- Formatting, static text health, and architecture regression guards passed after the Slice 7 documentation/rule update.
- The integration command discovers 309 real-PostgreSQL cases across 26 suites (API 156, worker 153), including the new actor-authority, ledger-rebinding, workflow, scheduler, crash-recovery, and completed-transport cases. They skip locally when `TEST_DATABASE_URL` is absent; the GitHub Actions integration job supplies PostgreSQL 17 and executes them.
- Post-review targeted ESLint passed for every changed runtime, persistence, fixture, and guard file. The broad repository lint command produced no findings but reached its 240-second local command limit; no child process from that command remained, and it was not launched again.

## Pause Checkpoint

The ADR 0022 implementation is committed as a review checkpoint rather than a completed production gate. Fresh closure review found bounded work still required before the milestone is called complete:

- reject loopback, private, link-local, and DNS-rebound website-import destinations;
- let an already-active workflow finish after pause while pause continues to block new admission;
- persist one transactionally authorized model-egress reservation per provider attempt and prevent silent knowledge-pointer rollback;
- seal each succeeded run to an immutable exact opportunity-projection manifest;
- propagate lease cancellation through public search and cancel non-success DeepSeek response bodies;
- move superseded migration guards to the latest installed definitions and correct the remaining coverage/count and shipped/deferred wording.

The partial closure batch already removes website-import artifact keys from browser responses, removes caller-selected screenshot storage keys from ranking-proof commands, strips both locator forms from legacy Scout model packets, and refreshes profile/knowledge/research queries after stale compare-and-set failures.

## Next

1. Finish the bounded closure list above without starting another product milestone.
2. Run the PostgreSQL 17 integration gate, authenticated browser flow, controlled DuckDuckGo smoke, and credentialed DeepSeek Opportunity Research smoke.
3. Choose the next product slice from observed workflow use; memory/RAG, browser action, broad MCP catalogs, and command execution remain need-triggered decisions rather than automatic follow-ons.
