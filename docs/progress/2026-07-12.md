# 2026-07-12

## Controlled Page Studio Editing Foundation

- Added a closed Page Studio command contract for structured section-prop replacement, legal section movement, and registry-owned variant switching. Arbitrary PageJson replacement and JSON Patch remain outside the boundary.
- Reused pure `packages/domain` Page Studio decisions and added command application with human generation provenance on the page and directly edited section.
- Added `page:edit` permission and a project-scoped edit endpoint that requires a persisted actor.
- Made edits append-only: every successful command creates the immediate N+1 `preview` version and records `basedOnVersionId` plus `createdByUserId`; the source version is never mutated.
- Serialized edit and review decisions on the page proposal so only the latest version can be edited or reviewed and concurrent same-base edits cannot both persist.
- Added a fail-closed direct-lineage backfill, DB-enforced same-proposal immediate lineage, and append-only structural/provenance enforcement for preview and immutable versions.
- Kept approved and released artifacts branchable: an edit creates a new preview while the frozen source stays unchanged.
- Ran contract/projection, registry, composition, and shared preview-render gates before edit persistence.
- Added contract, domain, permission, and DB-backed integration coverage for command validity, invalid edits, immutable branching, tenancy, actor evidence, concurrent edits, stale review, and lineage triggers.
- Verified the API Postgres integration suite locally: 96 tests passed.
- Verified the worker Postgres integration suite locally: 79 tests passed; the release supersede fixture now uses a real v1-to-v2 lineage chain.
- Verified the complete repository check, including formatting, architecture guards, lint, typecheck, migration drift, build, and unit tests.
- Updated ADR 0017, Page Studio architecture, roadmap, backend/frontend status, lifecycle coverage, and executable regression guards in the same slice.

Next:

- Broad-review this backend/versioning foundation before adding visual editor controls.
- Add the Page Studio outline and structured controls on top of the explicit command API; keep media and AI copy actions as separately bounded commands.
