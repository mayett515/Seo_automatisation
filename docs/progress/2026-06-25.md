# 2026-06-25 Progress

## Scope

Initial Local SEO platform scaffold hardening and rule-system setup.

## Completed

- Added portable hidden rule routing through `AGENTS.md`.
- Added Local SEO product rules, stack rules, Nest rules, Fastify rules, planner rules, stealer workflow rules, and diagram rules.
- Added Google Search Console OAuth/sync vertical slice.
- Added GSC DB schema, migrations, sync runs, Search Analytics rows, opportunity signals, and indexes.
- Added frontend GSC connect and performance dashboard screens.
- Added frontend Zod parsing, safe URL rendering, encoded project API paths, and explicit TanStack Query states.
- Added Prettier, ESLint, type-aware linting, React Hooks linting, and TanStack Query linting.
- Added real package/API/worker build topology using `tsc` emit and production export conditions.
- Added production-condition smoke checks for built API and worker artifacts.
- Moved human-readable ADRs to `docs/architecture/decisions/`.
- Added ADRs for Nest hardening, Fastify adapter boundaries, and Nest decorator metadata in backend builds.
- Added `QueueProducerService` so scaffold queue endpoints enqueue when Redis exists and return `dry_run` when it does not.
- Refactored GSC DB, queue, Search Console, and token cipher construction into Nest providers.
- Added `/health/live` and `/health/ready`.
- Upgraded readiness checks to real DB/Redis reachability.
- Added shared Redis URL parsing with `rediss://` TLS handling.
- Added GitHub Actions CI for format, whitespace, lint, typecheck, build, and test.
- Added project access guard scaffold for project-scoped API routes.
- Added behavioral tests for release readiness, release verification, Redis URL parsing, and project guard behavior.
- Adjusted CI whitespace checks to validate new working-tree changes without failing on frozen copied reference bundles.
- Ran the stealer workflow for production auth/tenancy using `C:\a good artist steals`, the local repo catalog, NestJS docs, Fastify docs, and Better Auth docs.
- Added ADR 0005 for the production auth boundary: Better Auth owns identity/session state, Nest guards own project/customer authorization, and Fastify owns adapter/runtime plumbing only.
- Updated Nest/Fastify rule sources and guard rules so future auth work routes through Better Auth, Nest guards, and Fastify adapter guidance.
- Added `text:check` and ADR 0006 so repeated review findings become executable CI guardrails, AI rules, and human-readable decision memory.
- Closed the release-route authorization gap found in review by making release endpoints project-scoped and making `ProjectAccessGuard` fail closed without project context.
- Added a customer membership table and contract-sourced `customer_membership_role` enum.
- Upgraded `ProjectAccessGuard` so UUID-like persisted project ids require database-backed membership checks; header project lists remain local scaffold behavior only for non-UUID ids.
- Added a public tracking ingestion boundary: persisted project tracking events require `TRACKING_INGEST_TOKEN`, while demo/local scaffold events stay usable.
- Added Fastify adapter hardening with body size limit, security headers, and a conservative global rate limit.
- Added focused tests for OAuth state signing/expiry/tampering, token cipher tampering, GSC sync job parsing, opportunity-signal classification, persisted-project guard behavior, and tracking ingestion rejection.
- Synthesized Opus/Gemini/GLM review findings into ADR 0007 and strengthened routed rules for auth, OAuth, deploy approval, tracking, queues, reporting, shared providers, and Fastify production topology.
- Added Good Artist Inspiration / pattern-mining routing, architecture decision domain scans, and category reminders for future architecture-significant slices.
- Closed Opus follow-up contradictions: `demo-project` no longer bypasses production guard/tracking boundaries, malformed user ids fail before persisted membership lookup, and API production boot now fails fast when required security/runtime env vars are missing.
- Added hard checking for DB migration drift, a root `check` command, a CI database migration check, and promoted `no-explicit-any` to an ESLint error.

## Verified

```text
corepack pnpm format:check
corepack pnpm lint
corepack pnpm text:check
git diff --check
corepack pnpm typecheck
corepack pnpm build
corepack pnpm test
node --conditions=production -e "import('./apps/api/dist/app.module.js')..."
node --conditions=production apps/worker/dist/main.js
```

## Remaining Next

- Wire project access guards to Better Auth/session-backed project membership before real customer data.
- Replace temporary user-header extraction with Better Auth session-backed user context.
- Add permission metadata for deploy/report/admin operations where role differences matter.
- Persist release approvals, verify deploy readiness from DB state, and resolve release-plan ownership before enqueueing deploy.
- Bind GSC OAuth callback state to the initiating app user/session and consume nonce state once.
- Move tracking from global ingestion token to per-project publishable keys and persist or explicitly dry-run accepted events.
- Add actor metadata to user-triggered queue jobs and job audit records.
- Expand behavioral tests for GSC finalized date ranges, queue producer behavior, and deploy worker idempotency.
- Expand readiness from DB/Redis to queue ability, provider availability, and worker reachability.
- Add deeper deployment smoke checks once deployment targets are concrete.
