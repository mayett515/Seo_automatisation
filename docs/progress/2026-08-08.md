# 2026-08-08

## CI Trigger-Error Assertion Repair

- Replaced the release integration suite's direct PostgreSQL message regex with the shared cause-chain matcher so postgres.js trigger errors remain visible through wrapped causes.
- Kept the asserted trigger text exact; no production behavior changed.
- Committed and pushed `6828b51`; GitHub Actions validate, browser-smoke, and integration jobs passed.

## ADR 0022 Agentic Runtime And Evidence Ledger

- Completed the focused Good Artist checkpoint from the existing Mastra/agent/evidence research and current official Mastra workflow, storage, observability, and tool documentation.
- Accepted ADR 0022 as a design-only slice. No migration, runtime, live tool, or provider code ships in this slice.
- Separated three authorities: SEO source truth, agent execution truth, and product result truth.
- Kept `agent_runs` as the top-level product workflow lifecycle and fixed the next persistence shape as typed steps, compact append-only events, immutable source-backed evidence items, and step-to-evidence links.
- Classified Mastra snapshots, storage domains, traces, and logs as operational runtime data. PostgreSQL remains authoritative for product recovery claims, evidence identity, approval, and results; BullMQ remains transport.
- Chose one purpose-named Opportunity Research workflow rather than a generic agent platform. Specialist Research/SEO Strategy agents are steps inside one run unless they gain an independently admitted product lifecycle.
- Accepted direct DeepSeek with a configurable Flash-class default as the next adapter direction. The current OpenCode Go path remains until parity tests and a credentialed smoke pass succeed.
- Kept persistent memory/RAG, broad MCP catalogs, autonomous browser action, Mastra-owned approval, and agent-owned production commands deferred.

## Next Slice

ADR 0022 Slice 1 is contracts and persistence only:

```text
agent_runs workflow identity
agent_run_steps
agent_run_events
agent_run_evidence_items
agent_run_step_evidence_links
DB transitions, immutability, tenancy, digests, and concurrent-claim tests
```

Direct DeepSeek and actual Mastra execution start only after that durable boundary is reviewed and committed.
