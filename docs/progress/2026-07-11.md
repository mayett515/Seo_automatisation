# 2026-07-11

## Recovery Milestone Review Hardening

- Reconciled the broad Fable review of commit `134ce04`; no blocking recovery defect was found.
- Isolated candidate loading per registered recovery lane so a Page Proposal query failure does not suppress release-verification scanning, or vice versa.
- Clarified that `WORK_RECOVERY_BATCH_SIZE` applies independently to each registered lane.
- Kept transport-completed inconsistency distinct from bounded recovery exhaustion in release-verification summary and check evidence.
- Added unit coverage for per-lane load isolation.
- Added DB-backed integration coverage for Page Proposal exhaustion, late transport coalescing after a durable claim, and completed-audit fallback after BullMQ job retention removes the live transport row.
- Recorded BullMQ terminal-job retention as a deliberate later operations decision; recovery currently preserves terminal evidence and safely removes same-id residue only when applying a guarded re-enqueue.
- Re-verified the prior release-plan cancellation concern: cancellation already persists an actor-backed `approvals` audit row with `decision_note = release_plan_cancelled`, and its API integration test asserts that evidence.

## Page Proposal Real-Provider Smoke Harness

- Added a contract-valid canonical Page Proposal example to the reasoning prompt so real providers receive the closed registry prop shapes without making prompt text authoritative.
- Made proposal, page, and initial-section generation provenance worker-owned; model-provided attribution is replaced with the durable `agent_runs.id` before QA and persistence.
- Added DB-backed integration coverage using the actual `OpenCodeGoReasoningAdapter` with a controlled HTTP response. The test proves task/policy serialization, all deterministic gates, and draft/preview-only persistence.
- Added a synthetic Page Proposal fixture seeder that refuses to reset immutable page versions.
- Added a public-API-to-BullMQ smoke runner that requires explicit OpenCode Go configuration, polls durable run truth, validates proposal/version state, and prints only redacted summaries.
- Refactored Opportunity Scout and Page Proposal smoke runners onto one shared env/polling/redaction helper.
- Added `tools/tsconfig.json` to the repository typecheck command so operational smoke tooling cannot silently drift.
- Added the credentialed runbook and sanitized review-artifact policy under `docs/testing/`.
- Verified the final slice with the complete repository `check`, the 79-test worker Postgres integration suite, the focused two-test Page Proposal Postgres suite, and two idempotent synthetic-fixture reset/seed runs.
- A credentialed Page Proposal run was not executed in this shell because no external provider env file or API key was configured. Mock remains the default.

## Page Proposal Smoke Review Hardening

- Reconciled the broad review of commit `8db2543`; no blocking provider-smoke defect was found.
- Added a DB-free worker test that pins the canonical Page Proposal example to Page Registry validation, Page Studio composition, and shared preview rendering.
- Made the smoke runner snapshot pre-existing fixture proposal ids before enqueue, so failed-run checks reject newly created product rows without misattributing older fixture residue.
- Added `--require-succeeded` for strict credentialed prompt/model calibration while preserving classified failures as valid adapter-boundary smoke evidence by default.
- Verified the hardening through the complete repository check; the worker unit suite now passes 99 tests with the canonical-example compatibility pin in the normal non-DB loop.

Next:

- Run and review one credentialed synthetic Page Proposal smoke when the external provider env is available.
- Continue with controlled Page Studio editing after the smoke boundary review.
