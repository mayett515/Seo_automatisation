# Progress - 2026-07-15

## Sandboxed Preview Capability Hardening

- Reconciled the Fable review of ADR 0020 slice 2; no blocker was found for the binary artifact, manifest, preview, or deploy boundaries.
- Closed the latent local-preview cookie seam before slice 3: capability cookies now remain `Secure`, `SameSite=None`, and `Partitioned` in every environment instead of falling back to `SameSite=Lax` locally.
- Added a real-network Chromium test with a cross-site preview server and `sandbox=""` iframe. It proves the document capability reaches sandboxed navigation and the document-issued `/assets` capability reaches an image request from the iframe's opaque origin.
- Kept the existing API Postgres tests as the authoritative proof for signed manifest authorization, exact path membership, and byte-integrity checks; the browser test owns cookie-policy behavior only.
- Made the slice-3 closure criterion explicit: PageJson media references become renderer selection truth, the relational projection remains retention/FK evidence, and rendering fails closed unless PageJson references, projection rows, and resolved manifest entries match exactly.
- Left the bounded 20-cookie manifest lookup and the five-minute document cookie at `Path=/` unchanged; both were reviewed as intentional low-risk tradeoffs.
- Re-ran the canonical repository check, all API/worker Postgres integration suites, and all five Chromium tests on the hardened exact tree.

Next:

- Send the focused follow-up to Fable before committing.
- Start ADR 0020 slice 3 only after that review: `ImageText`, strict PageJson media props, transactional projection maintenance, exact reference/manifest cross-checks, and explicit Page Studio upload/select/alt/focal-point application.

## Page Studio Media Placement

- Added the first registry-owned media section, `ImageText`, with strict opaque `PageMediaReference` props, deterministic responsive rendering, placement alt semantics, and normalized focal-point positioning.
- Made PageJson references renderer selection truth. Every page-version insert now persists the exact `page_version_media_assets` projection in the same transaction, and the shared resolver rejects missing or extra projection rows.
- Enforced lifecycle-aware selection: new edits may select only ready project assets, while a direct descendant may retain an archived asset already referenced by its base version.
- Added exact media gates to edit, approval, release planning, preflight, preview, section-copy validation, and deploy; immutable byte count and SHA-256 are rechecked before serving or provider handoff.
- Kept AI Page Proposal generation outside project media selection. A proposal containing asset references is rejected at the deterministic `media_selection` gate.
- Added the Page Studio media library and upload flow with durable polling, verified thumbnails, ready-only selection, archived-reference visibility, content/decorative alt controls, focal-point sliders, and one explicit complete-props command creating N+1.
- Added DB-backed lifecycle/projection/release/deploy tests, DB-free registry/renderer tests, and a Chromium test proving upload and selection remain staging until one explicit version command.
- Updated ADR 0017, ADR 0020, roadmap/status/frontend/testing docs, rule 15, and executable guards in the same slice.

Next:

- Send the complete ADR 0020 slice-3 implementation to Fable before committing.
- Keep physical quarantine/derivative cleanup, remote media import, stock search, galleries/video, and AI image generation deferred.
