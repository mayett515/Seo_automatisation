# Progress 2026-07-05

## SERP Snapshot Boundary

- Started the next search/SERP slice as a boundary and storage baseline, not live automation.
- Added normalized SERP contracts in `packages/contracts`:
  - `SerpScoutRequest`,
  - `SerpSnapshot`,
  - `SerpSearchResult`,
  - `SerpFeature`,
  - `SerpEngineError`,
  - `SerpArtifactRef`,
  - `SerpScoutFailureCode`.
- Added `SerpScoutPort.search(...)` in `packages/adapters` as the purpose-named read-only search boundary.
- Reserved read-only reasoning tool categories for future search/page tools:
  - `search_web`,
  - `read_public_page`.
- Added `serp_snapshots` persistence in `packages/db`:
  - project-owned source rows,
  - optional `agent_run_id` provenance,
  - query/search engine/device/locale/region/cache key,
  - normalized result, SERP feature, engine error, and artifact-ref JSON,
  - `captured_at` for proof freshness policy later.

## Design Clarification

- DeepSeek can drive the future SERP/search workflow:
  - plan queries,
  - choose nearby Orte/service combinations,
  - call read-only SERP tools,
  - interpret the results.
- DeepSeek does not create ranking truth by itself. The evidence boundary is the stored `serp_snapshots` row or artifact.
- CAPTCHA/anti-bot is modeled as `captcha_blocked`, a stop signal rather than something to bypass.

## Deferred At Boundary Commit

- Live DeepSeek/Gemini/model-search adapter.
- Competitor page extraction artifacts.
- Proof freshness enforcement in reports.
- Automatic customer-safe proof promotion from `serp_snapshot` rows.

## SERP Scout Worker Baseline

- Added `serp_scout` as a first-class job type and `serp-scout` as a worker/API queue.
- Added `MockSerpScoutAdapter` in `packages/adapters` so the SERP path can be exercised without live Google/search-provider calls.
- Wired `handleSerpScoutJob` in `apps/worker`:
  - parses typed job data,
  - calls `SerpScoutPort.search(...)`,
  - persists captured snapshots into `serp_snapshots`,
  - records failed snapshots with normalized engine errors,
  - retries transient provider failures,
  - treats not-configured, captcha, and policy failures as terminal.
- Added API enqueue support:
  - `POST /projects/:id/serp-scout/runs`,
  - guarded by `opportunity:run`,
  - `jobId = snapshotId`,
  - dry-run semantics when DB/Redis is absent,
  - `job_runs` audit via the existing queue producer.
- Added focused worker and API tests for parsing, route wiring, failure classification, mock capture, queue audit, and dry-run behavior.

## Still Deferred After Worker Baseline

- Live DeepSeek/Gemini/model-driven SERP orchestration.
- Real SERP provider/browser adapter.
- Competitor page extraction artifacts.
- Proof freshness enforcement in reports.
- Automatic customer-safe proof promotion from `serp_snapshot` rows.

## SERP Pre-Live Hardening

- Added a shared `buildSerpSnapshotCacheKey(...)` helper so mock adapters, workers, and future providers do not drift on cache identity.
- Added `adapter_invalid_snapshot` as a first-class SERP failure code.
- Changed the SERP worker to persist a failed snapshot and stop retrying when an adapter returns malformed snapshot output or a snapshot for the wrong project/id.
- Added regression coverage for malformed and wrong-id adapter snapshots.
- Added SERP enqueue failure coverage proving `QueueProducerService` marks the `job_runs` audit row failed when BullMQ `queue.add` throws.

## OpenCode Go Model Routing Research

- Recorded the Big Eater model-routing finding:
  - the current OpenCode Go adapter supports the `/chat/completions` family,
  - `deepseek-v4-flash`, `deepseek-v4-pro`, `glm-5.2`, `kimi-k2.7-code`, and `mimo-v2.5*` are available through that lane,
  - `minimax-m3` and the `qwen3.7*` family need a later `/messages` adapter.
- Adjusted docs so `deepseek-v4-flash` is the first practical real smoke/default for cheap repeated worker runs.
- Kept `deepseek-v4-pro` as the harder reasoning fallback.
- Kept `glm-5.2` as a strong candidate for page/layout/content/report-quality work instead of a product contract.
- Model routing remains runtime adapter policy; contracts, DB, UI, QA, and evidence truth stay model-neutral.

## SERP Provider Strategy Research

- Recorded the provider-strategy conclusion from Big Eater:
  - F.2 is an adapter/proof-policy decision, not an LLM web-search decision.
  - Dedicated SERP APIs were researched as technical alternatives, but they are rejected as the MVP path after the no-SERP-API decision.
  - Manual `ranking_proofs` remain customer-safe proof while GSC, technical audit, browser capture, Brave/Tavily/Gemini/model search, and `serp_snapshots` stay discovery/context/supporting evidence by default.
- Strengthened the F.1 guard:
  - not every `serp_snapshot` can support `proven_win`,
  - after ADR 0015, no `serp_snapshot` can support `proven_win` in the MVP unless a future ADR explicitly promotes a deterministic proof source.

## No Paid SERP API Decision

- Added ADR 0015 to prevent future drift back toward DataForSEO, SerpApi, Serper, SearchAPI, or proxy-based rank scraping as the default MVP plan.
- Reframed F.2 from provider selection to `TechnicalAudit + ManualProof + optional OperatorCapture`.
- Current proof split:
  - customer-safe proof: reviewed manual `ranking_proofs`,
  - internal/supporting evidence: GSC, website import, tracking, technical audit, model/web search, browser/operator capture artifacts, and `serp_snapshots`.

## F.1 Proof-Tier Hardening

- Hardened Opportunity Scout QA so `customer_safe_proof` is allow-listed to `ranking_proof` only for MVP.
- Replaced the GSC-specific containment gate with `proof_tier_containment`, so all current and future non-proof evidence sources are rejected if the model marks them as customer-safe proof.
- Updated the scout prompt to tell the model that SERP snapshots, browser captures, model search, GSC, tracking, and audits are supporting context only.
- Added regressions proving:
  - `serp_snapshot` cannot satisfy `proven_win`,
  - every non-`ranking_proof` `EvidenceRef.sourceType` is rejected when it claims `customer_safe_proof`.

## F.1 Supporting Evidence Wiring

- Loaded captured `serp_snapshots` into the Opportunity Scout evidence packet as `supporting_context` candidates only.
- Excluded failed snapshots from Opportunity Scout resolution.
- Kept `serp_snapshot` unable to carry `customer_safe_proof`; customer-safe proof remains manual `ranking_proof` only for MVP.
- Added integration coverage proving captured snapshots can support an accepted brief while failed snapshots stay out of the packet.

## F.3 Minimal Manual Proof Hardening

- Added `ranking_proof_status` with `reviewed | invalidated`.
- Added invalidation metadata to `ranking_proofs`:
  - `invalidated_at`,
  - `invalidated_by_user_id`,
  - `invalidation_reason`.
- Added `PATCH /projects/:projectId/ranking-proofs/:proofId/status` behind `opportunity:evidence`.
- Opportunity Scout now loads only reviewed and fresh ranking proof rows.
- Added regression coverage for invalidated and stale proof rows failing proof resolution.

## F.2 Technical Audit Baseline

- Added first technical audit persistence:
  - `technical_audit_runs`,
  - `technical_audit_findings`.
- Added `technical_audit` as job type, queue name, and `EvidenceRef.sourceType`.
- Added pure domain analyzer for the first own-site checks:
  - HTTP status,
  - indexability/noindex,
  - canonical,
  - title/meta/H1,
  - schema detection,
  - internal links,
  - crawl skips.
- Added worker route `technical-audit` using the existing `CrawlerPort`; the worker stores the crawl artifact key and persists findings.
- Added `POST /projects/:id/technical-audit/runs` behind `website:import`.
- Opportunity Scout now loads `technical_audit` findings as supporting context only.
- TechnicalAudit observes site issues and creates evidence rows; it does not create opportunities and never becomes ranking proof.
