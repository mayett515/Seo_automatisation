# 2026-08-04

## ADR 0021 Command-Offer Prerequisite: Release Plan Target CAS

- Added a DB-managed non-negative `page_versions.row_version`; inserts are forced to revision zero, every update advances it, and callers cannot forge revisions.
- Replaced release-plan id-only input with a strict unique target set carrying each displayed page-version status and row version.
- Release-plan creation locks project-owned page versions in stable id order, re-reads current state, applies a pure stale-first admission decision, and writes draft plan/item truth only when every target still matches and remains approved with approval evidence.
- Page versions remain the terminal class in the established release lock order. Creation does not acquire an existing release-plan lock after taking target locks.
- The Page preview submits the displayed revision and invalidates page-version queries after conflicts so stale UI state self-heals.
- Contract/domain tests pin the closed request and decision taxonomy. Real-PostgreSQL coverage pins insert/update revision ownership and a forced lifecycle-update versus planning lock interleaving with zero plan/item writes for the stale loser.
- This slice creates no report command offers, consent receipts, or dispatch rows. Both initial target-CAS prerequisites are now implemented; consequential report action truth remains the next separate slice.

## Release Page-Version Lock Ordering Follow-Up

- Closed the reviewed pre-existing sorted-versus-unsorted page-version lock seam without changing lifecycle behavior.
- Deploy approval now deduplicates and sorts release target ids before locking and projecting them to `release_candidate`.
- Release planning canonicalizes contract-valid UUID text before sorting, keeping JavaScript request ordering aligned with PostgreSQL `uuid` ordering even for uppercase input.
- The shared release-candidate demotion helper materializes its target set, locks page versions in ascending id order, and only then restores them to `approved`. Cancellation, re-preflight, deploy failure, verification failure, and rollback all reuse that helper.
- Real-PostgreSQL coverage holds the higher page-version lock, proves cancellation already owns the lower row while waiting, then releases both writers and verifies both versions are restored.

## Report API Cohesion Extraction

- Preserved `ReportsService` as the Nest-facing facade with its existing constructor and public method surface, so controllers, workers, and PostgreSQL integration fixtures keep the same boundary.
- Moved generation admission/draft persistence, review/artifact lifecycle, and publication/correction/published reads into three focused internal capabilities.
- Moved canonical snapshot/provenance projection, evidence source verification and locking, aggregate row locks, and immutable artifact-byte checks into one shared report aggregate store.
- Kept routes, permission decorators, queue handoff, schemas, exception messages, transaction bodies, statement order, and lock order unchanged.
- Relocated executable regression guards from the former monolithic module path to the capability or aggregate-store file that now owns each invariant; new guards keep the Nest module free of report transactions and pin all three facade delegations.
- External Opus review found no behavioral blocker. Follow-up hardening narrowed sixteen internal aggregate-store helpers back to module-private scope, separately guard-pinned candidate and published immutable-byte reads, strengthened facade guards to assert real delegation, and added a unit test for the extracted artifact-reader assertion.
- Focused API typecheck, API unit tests, report contract/domain tests, formatting, and ESLint pass locally. Real-PostgreSQL report integration remains CI-gated because `TEST_DATABASE_URL` is not configured locally.

## Module Cohesion Rule Clarification

- The 19-rule architecture-direction shard triggered a cohesion and attention review; that review identified modular-monolith ownership and capability extraction as an independently routable concern.
- Moved modular-monolith topology, DDD-lite bounded contexts, and the premature-microservices ban into terminal sibling `.ai-project-rules/14A-module-cohesion-and-capability-extraction.md`; no rule text was deleted.
- Kept composition-root wiring and shared enum/event/payload truth in rule 14 next to their Clean Architecture, dependency-ownership, and source-of-truth trigger gates.
- Added project-specific cohesion criteria to 14A: capability ownership, actor/permission, transaction root, lifecycle authority, dependencies, and change cadence identify extraction candidates; size alone does not decide a split.
- Made the decision explicitly non-forcing: reviews return `keep_cohesive`, `extract_capability`, or `defer_pending_evidence`, and a post-change audit checks stable facade, invariant ownership, behavior tests, and moved guards without treating extraction as the only successful result.
- Left generic file-splitting with the frozen `.ai-rules/06-modular-architecture.md`, `.ai-rules/01C-readable-code-for-humans-and-ai.md`, and `.ai-rules/09-anti-regression.md` instead of restating it in a project shard.
- Rewrote the project router condition so rule 14 owns dependency direction, ports/adapters, and composition roots while 14A owns cohesion, extraction, and modular-monolith topology.
- Added rule-routing and module-cohesion text anchors to `tools/check-architecture-regression-guards.ts`; the existing ReportsService facade and aggregate-store guards already cover the code seam and were not duplicated.
- Rule 14 retains 16 rules with `rule_budget: "cohesion-retained"` and a `<context>` rationale because moving shared type and payload truth would fragment one architecture-ownership decision. Rule 14A contains 13 independently routable module-cohesion rules.
- No ADR changed: this clarifies decomposition criteria inside the modular monolith accepted by ADR 0001 and `docs/architecture/app-blueprint.md`.

## Adaptive Schema Rule-Budget Policy

- Updated the external `C:\Scheme` master, operational flat TypeScript bundle, categorization planner, Codex schema factory, human guidance, and schema research explanation from a hard normal-domain ceiling to an adaptive review threshold.
- Fifteen atomic directives and constraints now trigger review of duplication, cohesion, routing precision, and attention density; count alone does not force a split or fail CI.
- Horizontal splitting is required only when every sibling owns an independently coherent and directly routable concern. Tightly related rules may remain together when splitting would fragment context, with `rule_budget: "cohesion-retained"` and the rationale recorded in `<context>`.
- Preserved `rule_budget: "guard-exception"` for intentionally larger router, guard, guardrail, and anti-regression shards whose enforcement would weaken if split.
- Synchronized the project active `.ai-rules`, planning router, and frozen schema-factory mirrors from the revised upstream sources. The product-pack `sources/schema_reference/` directory remains the as-uploaded historical snapshot and is not active schema authority.
- Updated the first-class Core Logic Learning AI OS sibling pack in `C:\Scheme`, including its machine-readable manifest and integrity checksums, so the master repository no longer ships a second hard-ceiling policy.
- Added policy-text regression anchors only; no automatic rule-count failure was introduced.
- Reviewed every currently active over-threshold shard found by the focused audit. Functional-library selection, Fastify production readiness, queue lifecycle, deployment, backend/worker/Mastra authority, tracking/privacy, GSC verification, opportunity planning, and TanStack adoption remain cohesive with file-specific rationales; the reusable local-page quality gate is the sole new `guard-exception` because partial loading would weaken enforcement.
- The active project `.ai-rules` consumer copy is now an intentional text-check dependency; changes to its upstream authoring authority must update the policy anchors in the same project sync.
- Reconciled a pre-existing `02C-type-source-of-truth-checker.md` mirror difference by promoting the project's generic soft-checker clarification into the external operational TypeScript schema; module-specific actor, transaction, and lifecycle criteria remain project-owned in rule 14A.
